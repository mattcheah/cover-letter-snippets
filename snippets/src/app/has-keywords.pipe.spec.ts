import { HasKeywordsPipe } from './has-keywords.pipe';

describe('HasKeywordsPipe', () => {
  it('create an instance', () => {
    const pipe = new HasKeywordsPipe();
    expect(pipe).toBeTruthy();
  });
});
